# Logging internal services endpoints
apiVersion: v1
kind: ConfigMap
metadata:
  name: logging-stack-cm
  namespace: fluent
data:
  lokiURL: "http://logging-loki-gateway.loki"
  elasticsearchHost: efk-es-http.elastic
  fluentdHost: fluentd
