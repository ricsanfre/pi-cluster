apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - prometheus-rule.yaml
  - grafana-dashboard.yaml

configMapGenerator:
  - name: kube-vip-helm-values
    behavior: merge
    files:
      - monitoring-values.yaml=values.yaml

patches:
- target:
    group: helm.toolkit.fluxcd.io
    kind: HelmRelease
    name: kube-vip
  path: helm-patch.yaml