apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

resources:
- external-nodes-scrapeconfig.yaml
- external-bind-scrapeconfig.yaml
- grafana-dashboards.yaml
# MinIO-specific monitoring removed (RustFS uses OTLP/OpenTelemetry push, not direct scrape):
# - minio-bearer-externalsecret.yaml
# - minio-ext-scrapeconfig.yaml
- openwrt-scrapeconfig.yaml