# kube-prometheus-stack helm values (disable-grafana)
# Disabling instalation of Grafana sub-chart
grafana:
  enabled: false
  # Disable deployment of kube-prometheus-stack grafana dashboards
  forceDeployDashboards: false
