apiVersion: v1
kind: Namespace
metadata:
  name: databases
  # Ambient mesh intentionally NOT enabled on this namespace.
  # The CNPG operator webhook and PostgreSQL instances use their own TLS;
  # ztunnel interception breaks admission webhook calls and instance
  # health checks (EOF / connection reset on ports 9443 and 8000).
