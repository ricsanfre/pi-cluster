apiVersion: v1
kind: Namespace
metadata:
  name: cnpg-system
  # Ambient mesh intentionally NOT enabled. The CNPG operator webhook runs its
  # own TLS; ztunnel interception breaks the TLS handshake, causing EOF errors
  # from the kube-apiserver on admission webhook calls.
