apiVersion: v1
kind: Namespace
metadata:
  name: databases
  # Ambient mesh label is managed by the app/components/istio component.
  # See: cloudnative-pg/app/components/istio/namespace-labels.yaml
  # A PERMISSIVE PeerAuthentication is used to allow non-mesh clients
  # (kube-apiserver webhook calls, CNPG health checks) to connect
  # without SPIFFE identity. See design/adr/0003-ambient-mesh-exclusions.md
