apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - external-secret.yaml

patches:
- target:
    group: apps
    kind: Deployment
    name: kafdrop
  path: deployment-patch.yaml