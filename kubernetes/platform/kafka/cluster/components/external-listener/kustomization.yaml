apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - certificate.yaml
  - gateway.yaml
  - tls-route.yaml

patches:
- target:
    group: kafka.strimzi.io
    kind: Kafka
    name: cluster
  path: cluster-patch.yaml