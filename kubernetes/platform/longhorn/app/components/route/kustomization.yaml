apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - oauth2-external-secret.yaml
  - security-policy.yaml

# patch values.yaml?
configMapGenerator:
  - name: longhorn-helm-values
    behavior: merge
    files:
      - route-values.yaml=values.yaml

patches:
- target:
    group: helm.toolkit.fluxcd.io
    kind: HelmRelease
    name: longhorn
  path: helm-patch.yaml