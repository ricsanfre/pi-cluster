apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Istio PeerAuthentication for Keycloak PostgreSQL cluster.
# PERMISSIVE mTLS on metrics port allows Prometheus to scrape
# from outside the Istio ambient mesh.

resources:
  - peerauthentication.yaml
