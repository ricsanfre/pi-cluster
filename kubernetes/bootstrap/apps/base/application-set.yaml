apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: apps
  namespace: argocd
spec:
  generators:
  - list:
     elements:
     - name: kafka
       namespace: kafka
       path: kubernetes/apps/kafka/overlays/prod
       syncWave: "'0'"
  template:
    metadata: 
      name: "{{name}}"
      namespace: argo-cd
      annotations:
        argocd.argoproj.io/sync-wave: "{{syncWave}}"
    spec:
      destination:
        namespace: "{{namespace}}"
        name: in-cluster
      project: picluster
      source:
        path: "{{path}}"
        repoURL: https://github.com/ricsanfre/pi-cluster
        targetRevision: master
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        retry:
          limit: 10
          backoff:
            duration: 1m
            maxDuration: 16m
            factor: 2
        syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
        - ApplyOutOfSyncOnly=true