# ExternalSecrets — cart-service (e-commerce namespace)
# Provisions: cart-service-oauth-secret, cart-service-kafka-secret, cart-service-valkey-secret
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cart-service-oauth-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: cart-service-oauth-secret
    creationPolicy: Owner
  data:
    - secretKey: CART_SERVICE_CLIENT_SECRET
      remoteRef:
        key: /e-commerce/keycloak/clients/cart-service
        property: CLIENT_SECRET
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cart-service-kafka-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: cart-service-kafka-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /kafka/cart-service
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cart-service-valkey-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: cart-service-valkey-secret
    creationPolicy: Owner
  data:
    - secretKey: VALKEY_PASSWORD
      remoteRef:
        key: /e-commerce/databases/valkey/cart-service
        property: CART_SERVICE_VALKEY_PASSWORD
