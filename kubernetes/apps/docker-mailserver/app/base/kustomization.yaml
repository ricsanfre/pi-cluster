---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helm.yaml
  - docker-mailserver-app-externalsecret.yaml
  - docker-mailserver-certificate.yaml

configMapGenerator:
  - name: docker-mailserver-helm-values
    files:
      - base-values.yaml=values.yaml

configurations:
  - kustomizeconfig.yaml
