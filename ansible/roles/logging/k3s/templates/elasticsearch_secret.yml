---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ efk_cluster_name }}-es-elastic-user"
  namespace: "{{ k3s_logging_namespace }}"
  # labels:
  #   common.k8s.elastic.co/type: "elasticsearch"
  #   eck.k8s.elastic.co/credentials: "true"
  #   eck.k8s.elastic.co/owner-kind: "Elasticsearch"
  #   eck.k8s.elastic.co/owner-name: "{{ efk_cluster_name }}"
  #   eck.k8s.elastic.co/owner-namespace: "{{ k3s_logging_namespace }}"
  #   elasticsearch.k8s.elastic.co/cluster-name: "{{ efk_cluster_name }}"
type: Opaque
data:
  elastic: "{{ efk_elasticsearch_passwd | b64encode }}"
