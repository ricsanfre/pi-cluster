[tools]
# Versiones específicas verificadas (Mayo 2026)
kubectl = "1.36.3"
flux2 = "2.9.4"
velero = "1.18.1"
istioctl = "1.30.4"
helm = "4.2.4"
opentofu = "1.12.6"

# Herramienta via GitHub Releases
"github:longhorn/cli" = "1.12.1"

# Versión más reciente estable
cilium-cli = "0.20.0"
