[tools]
# Versiones específicas verificadas (Mayo 2026)
kubectl = "1.36.2"
flux2 = "2.9.1"
velero = "1.18.1"
istioctl = "1.30.2"
helm = "4.2.2"

# Herramienta via GitHub Releases
"github:longhorn/cli" = "1.12.0"

# Versión más reciente estable
cilium-cli = "0.19.5"
