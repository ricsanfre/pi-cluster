[tools]
# Versiones específicas verificadas (Mayo 2026)
kubectl = "1.36.1"
flux2 = "2.8.8"
velero = "1.18.0"
istioctl = "1.30.1"
helm = "4.2.2"

# Herramienta via GitHub Releases
"github:longhorn/cli" = "1.11.2"

# Versión más reciente estable
cilium-cli = "0.19.4"
