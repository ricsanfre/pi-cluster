---

- name: Check linkerd istallation
  command:
    cmd: linkerd version
  register: linkerd_version_out

- debug:
    var: linkerd_version_out


- name: Linkerd pre-installation checks
  command:
    cmd: linkerd check --pre
  register: linkerd_check_out

- debug:
    var: linkerd_check_out
