apiVersion: v1
kind: Secret
metadata:
  name: longhorn-minio-secret
  namespace: longhorn-system
type: Opaque
data:
  AWS_ACCESS_KEY_ID: {{ minio_longhorn_user | b64encode }}
  AWS_SECRET_ACCESS_KEY: {{ minio_longhorn_key | b64encode}}
  AWS_ENDPOINTS: {{ minio_url | b64encode }}